syntax = "proto3";

enum Enum {
  LOW = 0;
  MEDIUM = 1;
  HIGH = 2;
  URGENT = 4;
}

message User {
  int32 id = 1;
  string name = 2;
  string email = 3;
}

message Notification {
  string priority = 1;
  string msg = 2;
}